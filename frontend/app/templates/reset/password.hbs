<div class="forgot-password">
    {{#if model.valid}} {{password-comp resetPwd=(action 'resetPwd')}} {{else}}
    <div class="ui hidden divider"></div>
    <h5>Token Invalidated</h5>
    {{/if}}
</div>
